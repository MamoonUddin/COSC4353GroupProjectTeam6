const express = require("express");
const pricingRouter = express.Router();

const pricing = require("../tst/pricing.json").pricing;

pricingRouter.get("/", (req, res) => {
    
   //PLACEHOLDER TODO
    
});